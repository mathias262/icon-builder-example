{
  "private": true,
  "name": "@advisa/icons",
  "version": "0.1.0",
  "scripts": {
    "start": "npm run build",
    "build": "NODE_ENV=production node scripts/build.js",
    "release": "NODE_ENV=production node scripts/publish.js"
  },
  "devDependencies": {
    "del": "^3.0.0",
    "fs-extra": "^5.0.0",
    "glob": "^7.1.2",
    "svgo": "^1.3.2"
  }
}
